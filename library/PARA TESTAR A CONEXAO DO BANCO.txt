include __DIR__. '/library/Connection.php';  // Certifique-se de incluir o arquivo da classe.

// Cria uma instância da classe DataBase.
$dataBase = new DataBase();

// Chama o método testConnection para verificar a conexão.
$dataBase->testConnection();


DROP TABLE PESSOA;

CREATE TABLE PESSOA (
	ID SERIAL PRIMARY KEY,
	NOME VARCHAR(255),
	CPF VARCHAR (14),
	TELEFONE VARCHAR (20), 
	SEXO VARCHAR (20)
);

SELECT * FROM PESSOA;


INSERT INTO pessoa (NOME, CPF, TELEFONE, SEXO)
	VALUES ('Filipe', 123.456.789-10, '(62) 99399-9607', 'masculino');


DELETE FROM PESSOA
WHERE ID  > 2;